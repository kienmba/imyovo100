"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function UpdatePasswordForm(){const[pw,setPw]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const s=createClient();const {error}=await s.auth.updateUser({password:pw});setBusy(false);if(error)return setMsg(error.message);setMsg("Đã cập nhật mật khẩu.");setTimeout(()=>location.href="/dashboard",800)}return <form className="card auth-card stack" onSubmit={submit}><h1>Mật khẩu mới</h1><input className="input" type="password" minLength={8} required placeholder="Tối thiểu 8 ký tự" value={pw} onChange={e=>setPw(e.target.value)}/><button className="btn btn-primary" disabled={busy}>{busy?"Đang lưu...":"Cập nhật mật khẩu"}</button>{msg&&<div className="notice">{msg}</div>}</form>}