"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function ForgotPasswordForm(){const[email,setEmail]=useState(""),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const s=createClient();const {error}=await s.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/auth/update-password`});setBusy(false);setMsg(error?error.message:"Đã gửi email đặt lại mật khẩu nếu tài khoản tồn tại.")}return <form className="card auth-card stack" onSubmit={submit}><h1>Quên mật khẩu</h1><input className="input" type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn btn-primary" disabled={busy}>{busy?"Đang gửi...":"Gửi link đặt lại"}</button>{msg&&<div className="notice">{msg}</div>}<a href="/" className="center muted">← Đăng nhập</a></form>}