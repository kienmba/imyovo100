import SiteHeader from "@/components/SiteHeader";import AdminImportForm from "@/components/AdminImportForm";import {requireAdmin} from "@/lib/auth";export default async function Page(){await requireAdmin();return <><SiteHeader admin/><main className="container page"><h1>Admin Content</h1><p className="muted">Quản lý curriculum 100 ngày × 8 nhóm người học. Service role chỉ chạy server-side.</p><AdminImportForm/></main></>}
