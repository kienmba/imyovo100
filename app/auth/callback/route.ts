import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
export async function GET(request:Request){const u=new URL(request.url),code=u.searchParams.get("code"),next=u.searchParams.get("next");if(code){const s=await createClient();await s.auth.exchangeCodeForSession(code)}const dest=next?.startsWith("/")&&!next.startsWith("//")?next:"/dashboard";return NextResponse.redirect(new URL(dest,request.url))}